import { motion } from 'framer-motion'

const money = (value) => value.toLocaleString('pt-BR', { style: 'currency', currency: 'BRL', maximumFractionDigits: 0 })
export default function FinanceTable({ rows, type, setType, search, setSearch, sort, setSort }) {
  const filtered = rows.filter((row) => (type === 'Todos' || row.type === type) && row.description.toLowerCase().includes(search.toLowerCase())).sort((a, b) => sort === 'desc' ? b.value - a.value : a.value - b.value)
  return <motion.div initial={{ opacity: 0, y: 16 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} className="mt-5 rounded-2xl border border-white/10 bg-white/5 p-3 backdrop-blur-xl sm:p-4"><div className="grid gap-2 sm:flex sm:flex-wrap"><input className="min-h-11 min-w-0 flex-1 rounded-lg border border-white/10 bg-black/20 px-3 py-2 text-base outline-none placeholder:text-slate-500 focus:border-blue-400/60 sm:min-w-[210px] sm:text-sm" placeholder="Buscar descrição..." value={search} onChange={(e) => setSearch(e.target.value)} /><select className="min-h-11 rounded-lg border border-white/10 bg-[#030712] px-3 py-2 text-sm text-slate-300" value={type} onChange={(e) => setType(e.target.value)}><option>Todos</option><option>Entrada</option><option>Despesa fixa</option><option>Despesa variável</option></select><button className="min-h-11 rounded-lg border border-white/10 px-3 py-2 text-sm text-slate-300 active:bg-white/10 sm:hover:border-blue-400/50" onClick={() => setSort(sort === 'desc' ? 'asc' : 'desc')}>Ordenar valor {sort === 'desc' ? '↓' : '↑'}</button></div><div className="mt-4 grid gap-2 sm:hidden">{filtered.map((row, index) => <article className="rounded-xl border border-white/10 bg-black/15 p-3" key={`${row.description}-${index}`}><div className="flex items-start justify-between gap-3"><div><span className="text-[11px] text-slate-500">{row.date} · {row.type}</span><h4 className="mt-1 text-sm text-white">{row.description}</h4></div><strong className="shrink-0 font-mono text-xs text-blue-200">{money(row.value)}</strong></div><div className="mt-2 flex gap-2 text-[10px] text-slate-400"><span>{row.category}</span><span>·</span><span>{row.status}</span></div></article>)}</div><div className="mt-4 hidden overflow-x-auto sm:block"><table className="w-full min-w-[720px] text-left text-xs"><thead className="sticky top-0 border-b border-white/10 bg-[#0a1020] text-slate-400"><tr><th className="px-3 py-3">Data</th><th>Categoria</th><th>Tipo</th><th>Descrição</th><th>Valor</th><th>Status</th></tr></thead><tbody>{filtered.map((row, index) => <tr className="border-b border-white/5 text-slate-300 transition-colors hover:bg-white/5" key={`${row.description}-${index}`}><td className="px-3 py-3">{row.date}</td><td>{row.category}</td><td><span className="rounded-full border border-blue-400/20 bg-blue-950/40 px-2 py-1">{row.type}</span></td><td>{row.description}</td><td className="font-mono text-blue-200">{money(row.value)}</td><td>{row.status}</td></tr>)}</tbody></table></div></motion.div>
}
